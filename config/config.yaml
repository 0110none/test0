app:
  name: "Face"
  version: "1.0.0"
  known_faces_dir: "data/known_faces"
  logo: "assets/logo.png"
  log_dir: "logs"

recognition:
  detection_threshold: 0.5
  recognition_threshold: 0.6
  max_batch_size: 8
  device: "cuda"  # or "cpu"
  analysis_enabled: true  # Enable age/gender estimation when supported
